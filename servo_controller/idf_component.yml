#servo_controller/idf_component.yml

version: "1.0.0"
description: "32 servo controller"
url: "https://github.com/ljohanon06/ServoControl"
dependencies:
  idf: 
    version: ">=5.5"
    #works with 6.0

targets:
  - esp32s3
  - esp32c3
  #ESP chips must have RMT + Sync Manager to work
